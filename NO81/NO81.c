// 809*??=800*??+9*?? 其中??代表的两位数, 809*??为四位数，8*??的结果为两位数，9*??的结果为3位数。求??代表的两位数，及809*??后的结果。

#include <stdio.h>

int main()
{
    int i;
    int x = 0;
    for (i = 10; i < 100; ++i)
    {
        if ((((809 * i) / 10000 == 0 && (809 * i) / 1000 != 0)) && ((9 * i) / 1000 == 0 && (9 * i) / 100 != 0) && 809 * i == 800 * i + 9 * i)
        {
            x = i;
        }
    }

    printf("%d\n", x);
    printf("%d", 809 * x);

    return 0;
}